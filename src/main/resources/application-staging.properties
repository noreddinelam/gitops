spring.datasource.url=${GITOPS_DATABASE_URL}
spring.datasource.username=${GITOPS_DATABASE_USERNAME}
spring.datasource.password=${GITOPS_DATABASE_PASSWORD}
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=20
